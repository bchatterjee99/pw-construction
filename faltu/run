#-*-sh-*-


flags=""
files="extra-constraint.c ilp.c"
files="distance.c ilp.c"

if gcc $files $flags; then
    echo -e "\n\n  compile hoe geche\n\n";
    ./a.out
else
    echo -e "\n\n  hocche na\n\n";
fi
